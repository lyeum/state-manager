-- --------------------------------------------------------------------
-- 1-4. 현재 Act 조회
-- 용도: UI 표시, 시나리오 진행 상황 확인
-- API: GET /state/session/{session_id}/act
-- --------------------------------------------------------------------

SELECT current_act
FROM session
WHERE session_id = $1;
