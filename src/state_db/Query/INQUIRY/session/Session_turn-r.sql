-- --------------------------------------------------------------------
-- 2-3. Session의 현재 Turn 정보 조회
-- 용도: 상태 변경 트랜잭션의 기준점 확인
-- --------------------------------------------------------------------

SELECT current_turn
FROM session
WHERE session_id = $1;
